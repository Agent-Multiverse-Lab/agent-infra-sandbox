// Package docker contains the planned OCI container baseline.
//
// Its first experiment must measure cold start, cleanup, resource enforcement,
// and network behavior before exposing it through labctl.
package docker
