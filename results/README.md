# Results policy

Large raw result files are ignored by default. Commit small summaries beside the
experiment that produced them, together with the environment metadata and the
exact command or script used to collect them.
